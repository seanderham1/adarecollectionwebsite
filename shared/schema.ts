/**
 * No live database. Enquiry data is stored in Google Sheets via Cloud Functions.
 * Previously unused tables (users with a plaintext password column, contacts,
 * exclusive_access) were removed so the Record of Processing Activities stays honest.
 */
export {};
